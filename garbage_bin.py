# Garbage bin for unused/retracted parts of code from mealplanner.py

# def get_int_input(prompt, default_value):
#     """Prompt the user for an integer input. Returns the default value if the user just presses Enter."""
#     while True:
#         user_input = input(prompt)
#         if not user_input:
#             return default_value
#         if user_input.isdigit():
#             return int(user_input)
#         else:
#             print("Invalid input. Please enter an integer.")

# number_of_people = get_int_input("How many people to feed? [default = 1]: ", 1)  # Redundant due to complexity of planning a breakfast, lunch, and dinner. Maybe later!

'''  
# Modified from list comprehension below to avoid duplicates in list.

exclusions = [intolerances_map[i] for i in intolerances_input.split() if i in intolerances_map and i not in exclusions]

# Please refer to empty list, set, and iteration below. 
'''

